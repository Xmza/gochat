Добро пожаловать на борт команды **GoChat**, коллега. Рад, что ты с нами.

Сразу к делу. Времени у нас мало (2 часа в день — это жесткий тайм-бокс), поэтому работаем сфокусировано. Я не потерплю спагетти-кода, глобальных переменных и игнорирования ошибок. Мы пишем продакшн-реди сервис, а не скрипт для лабораторной.

Твоя цель — вырасти. Моя цель — не дать тебе выстрелить себе в ногу и заставить думать архитектурно.

---

### 1. Архитектурное решение и Стек

Я утверждаю следующий стек и структуру. Менять без обсуждения нельзя.

**Технологии:**
- **Router:** `go-chi/chi` — он идиоматичен, легковесен и заставит тебя работать с `context`, а не прятать его, как делает Gin.
- **WebSocket:** `nhooyr.io/websocket` — более современная библиотека, чем Gorilla. Она лучше работает с контекстами и потокобезопасна "из коробки" (но расслабляться не стоит).
- **DB:** `pgx/v5` — самый производительный драйвер.
- **Config:** `ilyakaznacheev/cleanenv` или стандартный `os.Getenv` (на твой вкус, но лучше первый).
- **Logger:** `slog` (стандартный в Go 1.21+) — привыкай к стандарту.

**Структура проекта (Standard Go Layout + Clean Arch):**
Мы разделяем слои, чтобы бизнес-логика не знала о базе данных или HTTP.

```text
gochat/
├── cmd/
│   └── app/
│       └── main.go        # Точка входа. DI-контейнер, инициализация.
├── internal/
│   ├── config/            # Загрузка конфигурации
│   ├── domain/            # Models, Errors, Interfaces (Contract definitions)
│   │   ├── models.go
│   │   └── errors.go
│   ├── usecase/           # Бизнес-логика (Service layer)
│   ├── repository/        # Реализация интерфейсов хранилища (Postgres, Redis)
│   └── delivery/          # Транспортный слой
│       └── http/          # HTTP Handlers, Routing, Middleware
├── pkg/                   # Общие утилиты (Logger, Validator)
├── migrations/            # SQL миграции
├── docker-compose.yml
├── Makefile
└── go.mod
```

---

### 2. Бэклог (Epics)

Вот наше глобальное видение MVP.
1.  **E-1: Foundation** (Скелет, конфиги, логгер, Docker).
2.  **E-2: Authentication** (Регистрация, Login, JWT, Middleware).
3.  **E-3: Real-time Core** (WS Handshake, Pool соединений, Broadcast).
4.  **E-4: Scalability** (Redis Pub/Sub для масштабирования инстансов).
5.  **E-5: Persistence** (Сохранение истории чата в Postgres, пагинация).

---

### 3. Sprint 0: Initialization

Твоя задача на ближайшие пару дней — поднять инфраструктуру. Я хочу видеть, что приложение запускается, база крутится в докере, и сервер умеет корректно завершать работу (Graceful Shutdown).

Вот твои тикеты. Забирай в работу.

#### 🎫 **[GOCHAT-1] Init Project Structure**
*Приоритет: Blocker*
**Задача:** Создать структуру папок, инициализировать `go.mod`. Настроить линтер.
**Требования:**
- Использовать структуру, утвержденную выше.
- Добавить `.golangci.yml` (включить: `errcheck`, `gosimple`, `govet`, `staticcheck`, `unused`). Код должен быть чистым.
**DoD (Definition of Done):**
- Команда `golangci-lint run` проходит без ошибок.
- В репозитории есть скелет папок.

#### 🎫 **[GOCHAT-2] Docker Environment**
*Приоритет: High*
**Задача:** Написать `docker-compose.yml` для поднятия зависимостей.
**Требования:**
- Сервис `postgres` (версия 15+).
- Сервис `redis` (версия 7+).
- Пробросить порты локально.
- Данные должны сохраняться в volume (persistence).
**DoD:**
- `docker-compose up -d` поднимает оба контейнера.
- Можно подключиться к БД через DBeaver/pgAdmin и к Redis через CLI.

#### 🎫 **[GOCHAT-3] Basic HTTP Server & Graceful Shutdown**
*Приоритет: High*
**Задача:** Реализовать `main.go` и базовый HTTP сервер на `chi`.
**Требования:**
- Запуск HTTP сервера на порту из конфига.
- Реализовать **Graceful Shutdown**: при получении `SIGTERM` или `SIGINT` сервер должен дождаться завершения текущих запросов (с таймаутом, например, 5 сек) и закрыть соединения, прежде чем упасть. Это критично для продакшена.
- Хендлер `GET /health` возвращает 200 OK.
**DoD:**
- При нажатии Ctrl+C в логах вижу сообщение "Server stopping...", затем "Server exited properly".
- Endpoint `/health` доступен через cURL.

#### 🎫 **[GOCHAT-4] Configuration & Logging**
*Приоритет: Medium*
**Задача:** Настроить централизованный конфиг и структурное логирование.
**Требования:**
- Конфиг читается из `.env` файла или переменных окружения (структура: `Env`, `Port`, `DB_URL`, `Redis_Addr`).
- Логгер (`slog`) настроен: JSON формат для продакшена, Text формат для локальной разработки.
- Логгер прокидывается или доступен глобально (лучше через DI, но на старте можно и singleton, если обоснуешь).
**DoD:**
- При старте приложение пишет в лог текущий конфиг (без паролей!) и уровень логирования.

---

**Твои действия:**
Выбирай тикеты (рекомендую делать по порядку), пиши код. Как будешь готов — кидай ссылку на репозиторий (или код текстом, если он небольшой) с пометкой **"PR for Sprint 0"**.

И помни: я буду придираться к обработке ошибок. Никаких `_` вместо error.

Удачи. Время пошло.
